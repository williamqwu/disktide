[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fsmonitor-cli"
version = "0.2.7"
description = "Interactive terminal disk usage explorer"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
keywords = ["disk-usage", "filesystem", "terminal", "textual", "tui"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Filesystems",
]

dependencies = [
    "textual>=8.2,<9",
    "textual-plotext>=1.0,<2",
    "squarify>=0.4.0",
    "drawille>=0.2.0",
    "click>=8.0",
    "humanize>=4.0",
]

[project.urls]
Homepage = "https://github.com/williamqwu/fsmonitor-cli"
Repository = "https://github.com/williamqwu/fsmonitor-cli"
Issues = "https://github.com/williamqwu/fsmonitor-cli/issues"

[project.scripts]
fsmonitor = "fs_monitor.__main__:cli"
fsmonitor-cli = "fs_monitor.__main__:cli"

[tool.hatch.build.targets.wheel]
packages = ["src/fs_monitor"]

[dependency-groups]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "textual-dev>=1.0.0",
]
